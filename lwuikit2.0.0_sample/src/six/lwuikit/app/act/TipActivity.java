package six.lwuikit.app.act;import java.util.ArrayList;import java.util.List;import java.util.Timer;import six.lwuikit.R;import six.lwuikit.tip.Tip;import android.app.Activity;import android.os.Bundle;import android.view.View;import android.view.View.OnClickListener;public class TipActivity extends Activity implements OnClickListener {	String[] str = {"haha","hello world","hehe"};	int index = 0;	List<Timer> list = new ArrayList<Timer>();	@Override	protected void onCreate(Bundle savedInstanceState) {		super.onCreate(savedInstanceState);		setContentView(R.layout.act_tip);	}	@Override	public void onClick(View v) {		// TODO Auto-generated method stub		if(v.getId() == R.id.showtip){//			startTimer();			Tip.makeTip().showTip(str[add()%str.length]);		}else if(v.getId() == R.id.hidetip) {//			stopTimer();			Tip.makeTip().hideTip();		}	}//	private void startTimer() {//		TimerTask task = new TimerTask() {////			@Override//			public void run() {//				Tip.makeTip().showTip(str[add()%str.length]);//			}//		};//		Timer mTimer = new Timer();//		list.add(mTimer);//		mTimer.schedule(task, 50, 3200);//	}	private int add(){		synchronized (this) {			return index++;		}	}//	private void stopTimer() {//		if(null != list) {//			for(Timer temp : list){//				temp.cancel();//			}//			list.clear();//		}//	}}